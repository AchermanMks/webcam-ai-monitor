# Web框架
Flask>=2.0.0
Werkzeug>=2.0.0

# 计算机视觉
opencv-python>=4.5.0
Pillow>=8.0.0

# 深度学习和AI模型
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.35.0

# Qwen VL工具包 (需要单独安装: pip install qwen-vl-utils)
# qwen-vl-utils

# 数据处理
numpy>=1.21.0

# 网络请求和通信
requests>=2.25.0
urllib3>=1.26.0

# PTZ控制协议支持
zeep>=4.2.1              # SOAP/ONVIF协议支持
lxml>=4.9.0              # XML解析
xmltodict>=0.13.0        # XML转字典

# 系统工具
psutil>=5.8.0
ipaddress>=1.0.23        # IP地址验证 (Python 3.8+内置，但保留兼容性)

# 网络发现和扫描
python-nmap>=0.7.1       # 网络扫描 (可选，用于自动发现摄像头)
zeroconf>=0.39.0         # mDNS/Bonjour服务发现 (可选)

# 加密和认证
cryptography>=3.4.8     # 加密支持
pycryptodome>=3.15.0     # 额外加密支持 (某些摄像头需要)

# 配置管理
python-dotenv>=0.19.0
pyyaml>=6.0              # YAML配置文件支持 (可选)

# 开发和调试工具 (可选)
pytest>=6.2.0           # 测试框架
pytest-cov>=2.12.0      # 测试覆盖率

# 注意事项:
# 1. qwen-vl-utils 需要单独安装: pip install qwen-vl-utils
# 2. 如果需要GPU加速，请安装CUDA版本的PyTorch:
#    pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
# 3. 某些系统可能需要额外安装系统依赖:
#    Ubuntu/Debian: sudo apt-get install libgl1-mesa-glx libglib2.0-0 nmap
#    CentOS/RHEL: sudo yum install mesa-libGL glib2 nmap
# 4. ONVIF支持需要zeep库，某些摄像头可能需要特定的SOAP客户端配置
# 5. 网络发现功能需要管理员权限 (sudo)
# 6. 如果遇到SSL证书问题，可能需要配置证书验证或使用HTTP连接

# 可选依赖 (根据需要安装):
# ================================
# schedule>=1.2.0         # 定时任务支持
# APScheduler>=3.9.0      # 高级任务调度
# redis>=4.3.0            # Redis缓存支持
# celery>=5.2.0           # 异步任务队列