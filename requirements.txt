# Web框架
Flask>=2.0.0
Werkzeug>=2.0.0

# 计算机视觉
opencv-python>=4.5.0
Pillow>=8.0.0

# 深度学习和AI模型
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.35.0

# Qwen VL工具包 (需要单独安装: pip install qwen-vl-utils)
# qwen-vl-utils

# 数据处理
numpy>=1.21.0

# 系统工具
psutil>=5.8.0

# 开发工具 (可选)
python-dotenv>=0.19.0

# 注意事项:
# 1. qwen-vl-utils 需要单独安装: pip install qwen-vl-utils
# 2. 如果需要GPU加速，请安装CUDA版本的PyTorch:
#    pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
# 3. 某些系统可能需要额外安装系统依赖:
#    Ubuntu/Debian: sudo apt-get install libgl1-mesa-glx libglib2.0-0
#    CentOS/RHEL: sudo yum install mesa-libGL glib2